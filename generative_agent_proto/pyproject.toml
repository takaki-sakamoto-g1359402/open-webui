[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "generative-agent-proto"
version = "0.1.0"
description = "Local lightweight cognitive generative-agent prototype."
requires-python = ">=3.9"
dependencies = []

[tool.setuptools.packages.find]
include = ["generative_agents*"]
