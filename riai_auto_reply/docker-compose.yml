version: "3.9"
services:
  riai:
    build: .
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - ./riai_auto_reply.db:/app/riai_auto_reply.db
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000
