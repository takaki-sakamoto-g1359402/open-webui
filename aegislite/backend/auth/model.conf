[request_definition]
r = role, org, sensitivity, act

[policy_definition]
p = role, org, max_sensitivity, act

[policy_effect]
e = some(where (p.eft == allow))

[matchers]
m = r.role == p.role && r.org == p.org && int(r.sensitivity) <= int(p.max_sensitivity) && r.act == p.act
